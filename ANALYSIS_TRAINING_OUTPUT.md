# 训练输出分析

## 当前情况

从训练输出看：
- ✅ **真实标签有事件**：5个事件帧（9, 18, 34, 52, 66）
- ⚠️ **模型预测0个事件**：所有预测都是类别0
- 📊 **概率分布**：
  - 类别0（无事件）：0.85+ （很高）
  - 类别1（事件）：最大0.40，平均0.21（不够高）

## 诊断

这是**正常的学习过程**，但说明：

1. **模型在学习**：有概率输出，不是完全随机
2. **但还不够**：类别1的概率需要 > 0.5 才会被预测为事件
3. **需要更多训练**：或者更高的类别权重

## 解决方案

### 方案1: 增加事件类别权重（推荐）

当前权重是 `[1.0, 10.0]`，可以增加到 `[1.0, 20.0]` 或 `[1.0, 50.0]`：

```python
# 在 MD-FED/train_MD-FED.py 第337行
class_weights = torch.tensor([1.0, 20.0]).to(self._device)  # 从10增加到20
```

### 方案2: 继续训练观察

如果这是第一个epoch，继续训练几个epoch看看：
- 如果类别1概率逐渐提高 → 继续训练即可
- 如果一直很低 → 需要增加权重

### 方案3: 调整学习率

如果学习率太小，模型学习慢：
- 当前：0.001
- 可以尝试：0.002 或 0.005

### 方案4: 使用动态阈值（临时方案）

如果概率在0.3-0.5之间，可以临时降低预测阈值（不推荐，但可以测试）

## 建议的行动

1. **先增加权重到20倍**，重新训练
2. **观察几个epoch**，看类别1概率是否提高
3. **如果还是不够**，继续增加到50倍

## 预期进展

正常训练应该看到：
- Epoch 1: 类别1概率 0.2-0.4
- Epoch 5: 类别1概率 0.4-0.6
- Epoch 10+: 类别1概率 > 0.5，开始预测事件
